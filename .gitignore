node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.local
.vite
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vscode/*
!.vscode/extensions.json
.idea

# Module 14 — server-side persistence directory (JsonFileRepo default).
# Contents are local operational state (raw artifacts, jobs, review
# queue, canonical entities, sync checkpoints). Do not commit.
data/
