// TEMPORARY SHIM. The sector taxonomy now lives in
// src/reference/sectorCatalog.ts. This re-export keeps the mock fixtures still
// behind the Mock⇄Live toggle working. Delete this file when the toggle and the
// mocks are removed.
export { sectors } from '../reference/sectorCatalog'
